<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ruixue.sdk.demo.qoo">

    <application android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_default_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_default_icon"
        android:supportsRtl="true"
        android:theme="@style/ThemeTranslucent"
        tools:replace="android:allowBackup">
        <meta-data
            android:name="com.qooapp.APP_ID"
            android:value="SANDd42a9224fd6611ee8acd06a4094a134e"/>
        <meta-data
            android:name="com.qooapp.PUBLIC_KEY"
            android:value="@string/qoo_public_key"/>

        <!-- 可选， 配置后 返回的data数据为base64编码后的，调用方需要反编码 -->
        <meta-data
            android:name="com.qooapp.ENC_VERSION"
            android:value="2" />
        <activity android:name=".DemoActivity" android:screenOrientation="portrait" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>