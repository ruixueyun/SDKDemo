<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ruixue.sdk.demo.jiuyou">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_default_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_default_icon"
        android:supportsRtl="true"
        android:theme="@style/ThemeTranslucent"
        tools:replace="android:allowBackup">
        <meta-data
            android:name="uc_game_id"
            android:value="@string/uc_game_id" />

        <!-- 增加app拉起相关cn.uc.gamesdk.activity.PullupActivity的声明 -->
        <activity
            android:name="cn.uc.gamesdk.activity.PullupActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="PullupActivity"
            android:launchMode="singleTop"
            android:taskAffinity="cn.uc.gamesdk.demo.diff"
            android:theme="@android:style/Theme.Translucent"
            tools:node="replace"
            tools:replace="android:taskAffinity">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- 这里的格式为 ng + gameId -->
                <data android:scheme="ng3098544" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.ruixue.sdk.demo.jiuyou.DemoActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>